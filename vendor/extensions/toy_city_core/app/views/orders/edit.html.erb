<%= render :partial => "shared/checkout_workflow", :locals =>  {:icon => "cart", :text => "Shopping Cart"} %>
<div id="checkout_main">
	<% form_for(:order, :url => object_url, :html => { :method => :put}) do |f| %>
	  <%= render :partial => 'form', :locals => {:f => f} %>

		<div id="subtotal">
			<h3><%= "#{t("Subtotal")}: #{order_price(@order)}" %></h3><br />
		</div>
		<div id="cart_options">
		  <%= image_submit_tag 'buttons/update_cart.gif' %><span> or </span><%= link_to image_tag('buttons/checkout.gif') , checkout_order_url(@order) %>  
		</div>

		<p style="text-align:center;"><%=link_to t("<-- Continue Shopping"), root_path %></p>
 
	<% end %>
</div>
<div id="checkout_footer">&nbsp;</div>